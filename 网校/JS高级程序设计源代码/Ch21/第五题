top和left在移动块方面没有transform灵活，操作难度低，代码量小
transform 属性元素应用 2D 或 3D 转换。我们可以用它对元素进行旋转、缩放、移动或倾斜。含有多个属性，可以实现2D和3D的大多数动画效果
而top定义了一个定位元素的上外边距边界与其包含块上边界之间的偏移。left定义了定位元素左外边距边界与其包含块左边界之间的偏移。而left和top属性较少，只有4个属性值。只能进行2D转换