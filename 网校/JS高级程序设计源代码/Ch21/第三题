浏览器跨域就是通过本浏览器可以访问到其他服务器，实现资源共享，
跨域
1 可以通过CORS实现的，即使用自定义的HTTP头部让浏览器与服务器进行沟通，从而决定请求或响应是否成功，比如一个简单的GET或POST 请求，在发送请求前可以在头部附加一个Origin，以便服务器根据头部信息来决定是否响应
2 可以通过图像PING实现，图像PING可以与服务器进行单向跨域通信，请求的 数据通过查询字符串发送，响应可以是任意内容，通过图像PING，但浏览器的得不到具体的数据。
3 可以通过JSONP实现，它由回调函数和数据组成，可以得到其他服务器的响应数据
